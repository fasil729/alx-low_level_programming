#include "holberton.h"
#include <stdlib.h>

/**
 * argstostr - converts args to string
 * @ac: argument count
 * @av: array of arguments
 *
 * Return: pointer to stings
 */
char *argstostr(int ac, char **av)
{
	char *new_string;
	int i = 0;

	if (ac == 0 || av == NULL)
	{
		return (0);
	}
	return (new_string);
}
